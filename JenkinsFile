pipeline{
    agent any
    environment {
        PATH = "${PATH}:${getTerraformPath()}"
    }
    
    //comment
    stages{
        stage("terraform init and apply"){
            steps{
                sh"terraform init"
                sh"terraform apply"
            }
        }
    }
}

